<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ItInfoListDAO">
    <select id="getItInfoList" resultType="com.example.demo.dto.ItInfoListDTO">
        SELECT itInfoId, title, generateDate, views
        FROM ItInformation
    </select>
</mapper>
