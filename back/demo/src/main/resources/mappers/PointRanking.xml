<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.PointRankingDAO">
    <select id="Ranking" resultType="com.example.demo.dto.SignUpDTO">
        SELECT userId, name, point FROM users ORDER BY point DESC
    </select>
</mapper>